<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:defaultValue="4"
        android:entries="@array/pref_digits_per_row"
        android:entryValues="@array/pref_digits_per_row"
        android:key="@string/pref_key_digits_per_row"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_digits_per_row" />
</PreferenceScreen>